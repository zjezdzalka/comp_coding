#include<bits/stdc++.h>
using namespace std;
int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int n, ilosc_n, m;
	cin>>n>>m;
	int i;
	int price;
	char rodzaj;
	double wynik[n];
	for(i=0;i<m;++i){
		cin>>rodzaj>>price;
		switch(rodzaj){
			case 'W':
				break;
			case 'Z':
				break;
		}
	}

	return 0;
}