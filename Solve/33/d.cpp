#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(NULL);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;
    cin>>t;

    long long a,b,l,r;

    while(ttt--)|{
        cin>>a>>b>>l>>r;
        long long res == __gcd(a,b);
        
    }

    return 0;
}