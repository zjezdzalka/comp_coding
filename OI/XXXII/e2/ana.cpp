#include<bits/stdc++.h>
using namespace std;

int minimumLength(){ return -1; }

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	
    int n;
    cin >> n;

    string ana;
    cin >> ana;

    int minMaxLength = INT_MAX;
    int amountOfSub = 0;



	return 0;
}